server:
  port: 8024

spring:
  data:
    mongodb:
      uri: mongodb+srv://rodo:nttdata@cluster0.zjvp3.mongodb.net/service-product-savingaccount?retryWrites=true&w=majority
  cloud:
    discovery:
      enabled: true
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
            
eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://${eureka.instance.hostname}:8761/eureka/
